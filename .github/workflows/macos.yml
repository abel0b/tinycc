name: linux

on:
  push:
    branches: [ mob ]
  pull_request:
    branches: [ mob ]

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2

    - name: configure
      run: ./configure --cc=clang

    - name: make
      run: make

    - name: make test
      run: make test
